# TMJS — Twitter/X 媒体批量保存器

油猴（Tampermonkey）脚本。在 X / Twitter 用户主页一键记录并批量保存图片和视频，自动打包成 ZIP 下载。

## 功能

- 自动滚动采集，边滚边记录，无需手动翻页
- 图片和视频一起打包，按每包数量自动分片
- **已保存记忆**：保存过的媒体会记下来，下次不再重复
- 无效 / 过小文件自动过滤
- 推文上实时打角标，采集进度一目了然
- 满量自动保存，不用盯着
- 可视化设置面板，参数随时改
- **视频日期自动还原**：从视频 ID 的 Snowflake 时间戳还原成 `YYYY-MM-DD`

## 安装

1. 浏览器装好 [Tampermonkey](https://www.tampermonkey.net/)
2. 打开这个地址：

   ```
   https://raw.githubusercontent.com/Eiranya/Tampermonkey-JS/main/twitter-image-saver.user.js
   ```

3. Tampermonkey 会自动识别脚本，点「安装」即可

装过之后，Tampermonkey 会通过 `@updateURL` 自动检查更新。

## 使用

1. 打开任意 X 用户主页
2. 点右下角的「记录媒体」
3. 脚本开始自动滚动并采集，推文上会出现角标
4. 采够了再点一次，自动打包下载

按钮状态：

| 状态 | 显示 | 说明 |
|---|---|---|
| 空闲 | 图片图标 + 记录媒体 | 点击开始采集 |
| 录制中 | 实心方块 + 已记录 N | 点击停止并打包 |
| 打包/下载 | 下载图标 + 打包中… | 正在处理，请勿关闭页面 |

## 设置

点右下角的**三点按钮**展开面板，改动即时生效并保存：

| 项 | 默认 | 说明 |
|---|---|---|
| 图片最小体积 | 50 KB | 小于此值的图片视为无效，跳过 |
| 视频最小体积 | 20 KB | 小于此值的视频视为损坏，跳过 |
| 满量自动保存 | 200 个 | 记录到这个数量自动触发保存 |
| 每包数量 | 150 个 | 每个 ZIP 打包多少文件 |
| 下载并发 | 3 路 | 同时下载几个文件 |
| 滚动间隔 | 2000 ms | 自动滚动节奏（带随机抖动） |
| 视频日期 | 推文时间优先 | 另一选项「视频ID优先」，见下 |

面板底部显示当前已保存记录条数，可以一键清除（清除后下次会重新记录所有媒体）。

### 视频日期的两种口径

- **推文时间优先**（默认）：用推文发布时间，和图片的命名口径一致
- **视频ID优先**：用视频自身的上传时间。转发旧视频时，这个日期才是"视频什么时候拍的"

## 文件名规则

```
图片   2026-08-02_214530_01.jpg
视频   video_2026-08-02_01.mp4
```

- 图片：日期 + 精确到秒的时间 + 同条推文内的序号
- 视频：日期 + 同一天内的序号

## 已知限制

- HLS（m3u8）视频需要下载 init 段 + 全部分片后拼接，**这部分没做过充分的实机验证**。如果遇到长视频下下来不能播，可以到 Issues 反馈
- 只在 `x.com` 和 `twitter.com` 生效
- 已保存记录存在 Tampermonkey 里，换浏览器或清理数据会丢失

## 更新记录

### v1.3.1

- UI 改成 Twitter/X 原生风格：白色药丸按钮、三点「更多」图标、X 官方 SVG 图标
- 按钮图标从 Emoji 换成 SVG（Emoji 跨平台字形配色差异大）
- 设置面板抬高，展开时不再遮挡主按钮

### v1.3

- 视频文件名从 `video_<snowflake>_06` 改成 `video_2026-08-02_06`
- 序号规则修正：图片和视频分开分组编号（之前混编，且拿不到时间的视频全挤在一组，序号退化成全局流水号）
- 新增可视化设置面板，配置项即时生效并持久化
- 清除历史记录功能整合进面板，并显示当前条数

### v1.2

修了 4 个导致视频漏识别的缺陷：

1. `fetch` hook 因变量作用域错误从未生效（`const w` 声明在 try 块内，下面引用时抛 `ReferenceError` 被静默吞掉）
2. 只 hook 了 `JSON.parse`，但 X 的 GraphQL 走 `await response.json()` —— 那是 Blink 原生解析，不经过页面的 `JSON.parse`，导致滚动加载的视频大面积漏抓。补上了 `Response.prototype.json` hook
3. `handleVariants` 只认 `video/mp4`，HLS-only 视频（只有 m3u8）整条链路被丢弃。补上了 m3u8 解析
4. hook 装得太晚（`load` + 1.5s），首屏 GraphQL 早已返回。改成 `document-start` 立即安装

另外修了 3 个"抓到了但被静默丢掉"的问题：`getMediaSize` 不判 HTTP 状态（403 错误体的 `content-length` 被当成"文件太小"）、同一函数异常时 Promise 永不 resolve 导致保存卡死、视频最小体积 100KB 阈值过高。

## License

MIT
