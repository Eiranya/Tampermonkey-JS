### v1.3.0 界面微调（2026-08-31，版本号不变）

> 注：Debug 按钮图标改版方案待用户选定后再实施（未落地，故不记录）。

- **移除设置面板右上角 × 关闭按钮**：面板改由三种方式关闭——再点右下角浮动齿轮按钮 / 点击面板外部 / 油猴菜单「打开设置面板」。
- **元数据修正**：`@description` 移除早已删除的「自动滚动」描述，改为「自动巡览逐帖采集」。
- 文档：README 同步代码实际参数（每半小时请求预算 80 次 / 满量自动保存 50 帖 / 下载并发 1 路移入 Debug 高级参数等）。

## v1.3.0 (2026-08-31)

### 抗风控参数调整
- **下载间隔下限 `DOWNLOAD_DELAY_MIN_MS`：300ms → 400ms**（配合上限 5000ms，最低等待更不易被判定为脚本突发）。
- **下载间隔偏斜指数 `DOWNLOAD_DELAY_SKEW`：4 → 2**（内部常量，不进设置面板）：分布从“约 45% 间隔压在 0.3–0.5s 的密集簇”变为“约 15% <0.5s、约 36% <1s、约 32% >2.5s”，拉长间隔、增加偶发长停顿，节奏更贴近真人。
- **新增：下载顺序随机打乱（抗风控）**：保存时对所有待下载媒体做 Fisher–Yates 随机排序后再逐一下发，避免请求按采集顺序规律排列。内部开关 `SHUFFLE_DOWNLOAD_ORDER`（默认 true，置 false 可恢复原始顺序，仅供调试）。
- **综合效果**：相比 v1.2.2，100 次下载平均等待约 193s（原 124s），但亚秒突发（<0.5s）从 45% 降到约 15%，长尾（>2.5s）更厚，整体更抗风控。
- 文档：README 同步更新参数与机制说明。

# 更新日志

> 🤖 **本仓库两个脚本（`twitter-image-saver.user.js`、`instagram-image-saver.user.js`）均由人工智能（AI）制作完成** —— 需求拆解、架构设计、编码实现、离线单元测试与实机问题定位均由 AI 辅助完成并逐轮迭代。

本文件记录仓库中**两个脚本**的版本更新历史，按脚本分节。

---

## 📷 instagram-image-saver.user.js（Instagram 媒体批量保存器）

### v1.2.2（2026-08-31）

修复 + 下载节奏人性化：

- **修复与测试修复**（14 条历史失败断言全部清零，10 套回归 371 断言 0 失败）
  - 测试桩与实现对齐：严格短码门控（v1.1.27）后单测的 `location` 停在网格页导致 `ingestBundle` 返回 0 → 测试补设帖子页 URL；大视频分流（v1.1.22）后未探测体积的视频进 ZIP → 测试桩补 `probedSize`；HEAD 小值防误杀守卫（v1.1.7）→ 测试改用 ≥50KB 的 Content-Length 并补 Range 分支桩
  - 单测 `reset()` 补清基址去重映射 `mediaBaseKey`（脚本本体本就成对清空，无真实泄漏）
  - 经逐条确诊，**14 条失败均为测试桩未跟上实现演进，脚本本体无功能缺陷**
- **下载请求随机间隔（新功能）**
  - 每次下载类请求（HEAD/Range 体积探测、媒体 Blob 拉取、大视频 GM_download，含失败重试）发出前随机等待
  - 间隔在 [下限, 上限] 内呈**偏态分布**（`rand^4`）：短间隔为主、长间隔稀少，避免请求集中触发风控；默认 300ms–5000ms
  - 新增常量 `DOWNLOAD_DELAY_MIN_MS` / `DOWNLOAD_DELAY_MAX_MS`（偏态分布曲线为内部固定参数，不进设置面板），min>max 自动钳制，两者均 ≤0 时关闭
  - 设置面板 Debug 模式新增「下载间隔下限 / 上限」两项可实时调整
  - 间隔关闭时保持与 v1.2.1 完全一致的同步调用语义
- 新增第 10 轮探针 `_probe_r10.js`：分布特性（边界 / 偏态 / 钳制 / 关闭开关）+ 双通道请求间隔实测（Blob 与 GM_download 相邻请求间隔均 ≥ 下限）

### v1.2.1（2026-08-31）

采集策略整体收紧，进一步降低触发 Instagram 风控的概率：

- **限流参数下调**
  - 请求预算窗口由「每小时」改为**每半小时**，数值 180 → **80**（实际速率上限下降约 55%）
    - 窗口时长提为内部常量 `BUDGET_WINDOW_MS`（30 分钟），面板标签与实际行为严格一致，不再两处各写一份
  - 满量自动保存阈值 100 帖 → **50 帖**，单轮采集规模减半，出问题时的损失面也更小
- **下载并发串行化**
  - 默认值 3 路 → **1 路串行**：串行拉取对 Instagram CDN 更像真人，代价是打包速度变慢
  - 「下载并发」设置项移入 **Debug 高级参数**（默认隐藏），普通设置面板不再显示
- **测试**
  - 新增预算窗口常量断言与「未满一个窗口不翻转」边界断言，防止面板标签与实际窗口时长脱节

### v1.2.0（2026-08-31）

首个对外发布版本（`main` 分支）。以「原图优先 + 只采当前帖 + 保存时统一打包」为核心：

- **原图获取修复**
  - 高清替换基址改用 `ig_cache_key`（IG 内容签名）归一化：同一张图无论尺寸档、请求签名（`_nc_gid`/`_nc_ss`/`_nc_ht`）如何变化都能正确合并，640 缩略图被 2048 原图替换
  - 详情页 JSON 缺失时**主动请求补齐**（`/p/{sc}/?__a=1`）：修复从主页 feed 点开帖子时 IG 复用缓存、不再请求详情数据导致动态视频 / 原图无来源的问题
- **采集范围严格化**
  - `ingestBundle` / `ingestItems` 一律**短码严格匹配当前页**，过滤主页 feed、相关网格等所有旁支
  - 单帖页 DOM 兜底容器**两种模式统一**为 `main article` → 回退 `main`，不再回退 `body`
- **下载策略**
  - 浏览过程中**不下任何文件**；保存时图片 + 小视频（< 阈值）进 ZIP（`images/` + `videos/`），大视频（≥ 阈值）单独下载并带失败自动重试
  - ZIP 按体积或数量自动分卷
- **配置整合**
  - 「缓存上限」与「满量自动保存」合并为一项，按**帖子数**（默认 100 帖）触发
  - 图片 / 视频最小体积合并为统一「媒体最小体积」（默认 50 KB）
  - ZIP 分卷阈值、视频打包阈值移入 Debug 模式（默认隐藏）
  - 新增**保存前预算预判**：估算完成保存所需请求，剩余预算不足时提前终止采集并保存
- **UI / 调试**
  - 设置面板标题栏 🔧 按钮 → Debug 浮动面板（显示**脚本版本号** + Debug 开关 + 导出诊断 JSON）
  - Debug 开关即时生效（高级参数与导出按钮无需重开面板）
- **数据源覆盖**
  - 新增 XHR 响应文本探针：IG 详情页数据走 XMLHttpRequest 路径时也能捕获
  - `web_info` 载荷优先入队处理；诊断捕获对 `web_info` 豁免上限（便于排查）
  - 视频字段变体识别（`video` 内嵌对象 / `playback_scenario`）

### 主要历史迭代（v1.1.x 精选）

- **v1.1.22–v1.1.23**：大视频单独下载恢复（保存时分流）；`ig_cache_key` 归一化修复原图替换失效
- **v1.1.19–v1.1.21**：满量自动保存改按帖子数；保存前预算预判；取消视频即时下载（避免浏览中反复弹窗）
- **v1.1.16–v1.1.18**：JSON 处理队列化（详情页并发载荷不丢）；高清替换（先缩略图后原图）；ZIP 按体积 / 数量分卷
- **v1.1.14–v1.1.15**：媒体候选只取最高清一档（避免同图多档重复）；巡览容器限定 `main article`；视频帖不早采（等待真实主媒体）；评论头像过滤
- **v1.1.10–v1.1.13**：自动巡览（打开帖子 → 采集 → 切下一帖 → 末页停止）；巡览门控（未进帖不采集）；设置面板开关持久化
- **v1.1.1–v1.1.9**：JSON 优先解析（内嵌数据 + API 响应）、增量去重、断点续抓、请求预算与风控熔断、小文件过滤（HEAD/Range 预筛 + 下载后复核）、自动排除快拍 / 精选封面、可视化设置面板

---

## 🐦 twitter-image-saver.user.js（Twitter/X 媒体批量保存器）

### v1.4.0（2026-08-29）

新增「空闲自动停止」功能：

- 记录过程中定时轮询已记录数量，若在设定的时间窗口内数量持续没有增长，则自动结束记录
- 新增两个脚本顶部可配置项（带合理默认值）：
  - `IDLE_TIMEOUT_MS`：空闲超时窗口，默认 `60 * 1000`（60 秒）；设为 `0` 可关闭该功能
  - `IDLE_CHECK_INTERVAL_MS`：轮询间隔，默认 `5 * 1000`（5 秒）
- 实现细节：
  - 已记录数量取自 `state.media.size`（Map 天然去重）
  - 新增内部状态 `lastCount` / `lastGrowthAt` / `idleTimer` 及 `startIdleWatch()` / `stopIdleWatch()`
  - 复用既有「满量自动保存」收尾逻辑：置 `state.autoSaving = true` 后延迟调用 `stopAndSave(true)`
  - `startRecording()` 启动轮询，`stopAndSave()` 停止轮询
- 约束：仅改动实现该功能所必需的代码，未更换依赖、未重构无关逻辑、未改动原有输出格式

### v1.3.1

- UI 改成 Twitter/X 原生风格：白色药丸按钮、三点「更多」图标、X 官方 SVG 图标
- 按钮图标从 Emoji 换成 SVG（Emoji 跨平台字形配色差异大）
- 设置面板抬高，展开时不再遮挡主按钮

### v1.3

- 视频文件名从 `video_<snowflake>_06` 改成 `video_2026-08-02_06`
- 序号规则修正：图片和视频分开分组编号（之前混编，且拿不到时间的视频全挤在一组，序号退化成全局流水号）
- 新增可视化设置面板，配置项即时生效并持久化
- 清除历史记录功能整合进面板，并显示当前条数

### v1.2

修了 4 个导致视频漏识别的缺陷：

1. `fetch` hook 因变量作用域错误从未生效（`const w` 声明在 try 块内，下面引用时抛 `ReferenceError` 被静默吞掉）
2. 只 hook 了 `JSON.parse`，但 X 的 GraphQL 走 `await response.json()` —— 那是 Blink 原生解析，不经过页面的 `JSON.parse`，导致滚动加载的视频大面积漏抓。补上了 `Response.prototype.json` hook
3. `handleVariants` 只认 `video/mp4`，HLS-only 视频（只有 m3u8）整条链路被丢弃。补上了 m3u8 解析
4. hook 装得太晚（`load` + 1.5s），首屏 GraphQL 早已返回。改成 `document-start` 立即安装

另外修了 3 个"抓到了但被静默丢掉"的问题：`getMediaSize` 不判 HTTP 状态（403 错误体的 `content-length` 被当成"文件太小"）、同一函数异常时 Promise 永不 resolve 导致保存卡死、视频最小体积 100KB 阈值过高。
